namespace Retro.Net.Z80.Core
{
    public static class CpuCoreMessages
    {
        /// <summary>
        /// A request to pause the CPU core.
        /// </summary>
        public const string PauseCpu = nameof(PauseCpu);

        /// <summary>
        /// A request to resume the CPU core.
        /// </summary>
        public const string ResumeCpu = nameof(ResumeCpu);
    }
}
